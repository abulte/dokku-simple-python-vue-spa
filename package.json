{
  "name": "dokku-simple-python-vue-spa",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "yarn install --dev --cwd frontend && yarn --cwd frontend run build"
  },
  "engines": {
    "node": "13.x",
    "yarn": "1.x"
  }
}
